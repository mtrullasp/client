import * as React from "react";
interface IProps {
    content: JSX.Element;
}
declare class TitleBar extends React.Component<IProps, {}> {
    constructor(props: IProps, context: any);
    render(): JSX.Element;
}
export default TitleBar;
